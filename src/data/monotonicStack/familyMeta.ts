import type { FamilyMeta } from '../../types/leafEnhancement'

export const FAMILY_META: Record<string, FamilyMeta> = {
  'fundamentals-ms': {
    tagline: 'Core monotonic stack operations: increasing/decreasing stacks, next/previous element queries.',
    keywords: ['monotonic stack', 'increasing stack', 'decreasing stack', 'next greater', 'next smaller', 'previous greater', 'previous smaller'],
    budget: ['increasing', 'decreasing', 'next', 'previous'],
  },
}
