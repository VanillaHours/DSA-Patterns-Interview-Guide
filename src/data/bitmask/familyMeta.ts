import type { FamilyMeta } from '../../types/leafEnhancement'

export const FAMILY_META: Record<string, FamilyMeta> = {
  'fundamentals-bm': {
    tagline: 'Core bitmask operations: representation, bit ops, iteration, and built-in functions.',
    keywords: ['bitmask', 'bit operations', 'subset', 'mask iteration', 'popcount', 'lowbit', 'bit functions'],
    budget: ['representation', 'bit ops', 'iteration', 'functions'],
  },
}
